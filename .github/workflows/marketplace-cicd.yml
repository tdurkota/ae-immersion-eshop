name: Marketplace CI/CD

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

env:
  REGISTRY: ${{ secrets.ACR_REGISTRY_NAME }}.azurecr.io
  ASPIRE_CLI_VERSION: "latest"
  DOTNET_VERSION: "10.0.x"

jobs:
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore dependencies
        run: dotnet restore

      - name: Build solution
        run: dotnet build --configuration Release --no-restore

      # TODO: Enable tests after initial deployment is working
      # - name: Run unit tests
      #   run: dotnet test tests/**/*UnitTests.csproj --configuration Release --no-build --verbosity normal --logger "trx;LogFileName=test-results.trx"

      # - name: Run functional tests
      #   run: dotnet test tests/**/*FunctionalTests.csproj --configuration Release --no-build --verbosity normal --logger "trx;LogFileName=test-results.trx"
      #   continue-on-error: true

      # - name: Upload test results
      #   if: always()
      #   uses: actions/upload-artifact@v3
      #   with:
      #     name: test-results
      #     path: '**/test-results.trx'

      # - name: Publish test results
      #   if: always()
      #   uses: dorny/test-reporter@v1
      #   with:
      #     name: Test Results
      #     path: '**/test-results.trx'
      #     reporter: 'dotnet-trx'
      #     fail-on-error: false

  # TODO: Enable security scanning after initial deployment is working
  # security-scan:
  #   name: Security Scan
  #   runs-on: ubuntu-latest
  #   permissions:
  #     contents: read
  #     security-events: write

  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     - name: Setup .NET
  #       uses: actions/setup-dotnet@v4
  #       with:
  #         dotnet-version: ${{ env.DOTNET_VERSION }}

  #     - name: Run Trivy vulnerability scanner
  #       uses: aquasecurity/trivy-action@master
  #       with:
  #         scan-type: 'fs'
  #         scan-ref: '.'
  #         format: 'sarif'
  #         output: 'trivy-results.sarif'

  #     - name: Upload Trivy results to GitHub Security
  #       uses: github/codeql-action/upload-sarif@v2
  #       if: always()
  #       with:
  #         sarif_file: 'trivy-results.sarif'
  #         category: 'trivy'

  build-docker-images:
    name: Build Docker Images
    runs-on: ubuntu-latest
    needs: [ build-and-test ]
    if: github.event_name == 'push'
    permissions:
      contents: read
      packages: write

    strategy:
      matrix:
        service:
          - Basket.API
          - Catalog.API
          - Identity.API
          - Ordering.API
          - WebApp
          - OrderProcessor
          - PaymentProcessor
          - Webhooks.API

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Azure Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/eshop-${{ matrix.service }}
          tags: |
            type=ref,event=branch
            type=sha,prefix={{branch}}-

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./src/${{ matrix.service }}/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    needs: [ build-and-test, build-docker-images ]
    if: github.ref == 'refs/heads/develop' && github.event_name == 'push'
    environment:
      name: staging
      url: https://eshop-staging.azurecontainerapps.io

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Install Aspire CLI
        run: dotnet tool install -g Aspire.Cli --version ${{ env.ASPIRE_CLI_VERSION }}

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy to Azure Container Apps (Staging)
        run: |
          aspire publish --from-module ${{ secrets.ASPIRE_MODULE_PATH }} \
            --provision-if-not-exists \
            --subscription-id ${{ secrets.AZURE_SUBSCRIPTION_ID }} \
            --resource-group ${{ secrets.AZURE_RESOURCE_GROUP }} \
            --location ${{ secrets.AZURE_LOCATION }} \
            --environment-name eshop-staging
        continue-on-error: true

  deploy-production:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: [ build-and-test, build-docker-images ]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    environment:
      name: production
      url: https://eshop-marketplace.azurecontainerapps.io
    concurrency:
      group: production-deployment
      cancel-in-progress: false

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Install Aspire CLI
        run: dotnet tool install -g Aspire.Cli --version ${{ env.ASPIRE_CLI_VERSION }}

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy to Azure Container Apps (Production)
        run: |
          aspire publish --from-module ${{ secrets.ASPIRE_MODULE_PATH }} \
            --provision-if-not-exists \
            --subscription-id ${{ secrets.AZURE_SUBSCRIPTION_ID }} \
            --resource-group ${{ secrets.AZURE_RESOURCE_GROUP }} \
            --location ${{ secrets.AZURE_LOCATION }} \
            --environment-name eshop-production
        continue-on-error: true

  # TODO: Enable E2E tests after initial deployment is working
  # e2e-tests:
  #   name: E2E Tests (Marketplace Flow)
  #   runs-on: ubuntu-latest
  #   needs: [ deploy-staging ]
  #   if: github.ref == 'refs/heads/develop'
  #   
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #         cache: 'npm'

  #     - name: Install Playwright dependencies
  #       run: npx playwright install --with-deps

  #     - name: Run E2E tests
  #       run: npm run test:e2e
  #       env:
  #         STAGING_URL: https://eshop-staging.azurecontainerapps.io

  #     - name: Upload E2E test results
  #       if: always()
  #       uses: actions/upload-artifact@v3
  #       with:
  #         name: e2e-test-results
  #         path: test-results/

  # notify-linear:
  #   name: Notify Linear
  #   runs-on: ubuntu-latest
  #   needs: [ build-and-test ]
  #   if: always()
    
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     - name: Notify Linear of Build Status
  #       run: |
  #         curl -X POST ${{ secrets.LINEAR_API_ENDPOINT }} \
  #           -H "Authorization: Bearer ${{ secrets.LINEAR_API_KEY }}" \
  #           -H "Content-Type: application/json" \
  #           -d '{
  #             "query": "mutation CreateComment($issueId: String!, $body: String!) { issueAddComment(issueId: $issueId, body: $body) { success } }",
  #             "variables": {
  #               "issueId": "${{ secrets.LINEAR_BUILD_ISSUE_ID }}",
  #               "body": "Build ${{ github.run_id }} - Status: ${{ job.status }} - Commit: ${{ github.sha }}"
  #             }
  #           }'
  #       continue-on-error: true

  # TODO: Enable marketplace features build after initial deployment is working
  # build-marketplace-features:
  #   name: Build Marketplace Features
  #   runs-on: ubuntu-latest
  #   needs: [ build-and-test ]
  #   if: contains(github.head_ref, 'feature/marketplace')

  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     - name: Setup .NET
  #       uses: actions/setup-dotnet@v4
  #       with:
  #         dotnet-version: ${{ env.DOTNET_VERSION }}

  #     - name: Build Marketplace Services
  #       run: |
  #         dotnet build src/VendorManagement.API/VendorManagement.API.csproj --configuration Release
  #         dotnet build src/CommissionEngine.API/CommissionEngine.API.csproj --configuration Release
  #         dotnet build src/MarketplaceSearch.API/MarketplaceSearch.API.csproj --configuration Release
  #       continue-on-error: true

  #     - name: Test Marketplace Services
  #       run: |
  #         dotnet test tests/VendorManagement.UnitTests/ --configuration Release --no-build
  #         dotnet test tests/CommissionEngine.UnitTests/ --configuration Release --no-build
  #         dotnet test tests/MarketplaceSearch.UnitTests/ --configuration Release --no-build
  #       continue-on-error: true
